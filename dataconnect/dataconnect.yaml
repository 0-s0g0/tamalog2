specVersion: "v1beta"
serviceId: "reiapp"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "reiapp-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
